#Область ОбработкаПроведения

Процедура ОбработкаПроведения(Отказ, Режим)
	
	// 1. Инициализация набора записей
	Движения.ВКМ_ОсновныеНачисления.Записывать = Истина;
	Движения.ВКМ_ОсновныеНачисления.Очистить();
	
	// 2. Формирование движений (с учетом истории окладов!)
	// Передаем Отказ, чтобы прервать проведение при ошибках
	СформироватьДвиженияПоОкладам(Отказ);
	
	// 3. Добавление остальных начислений (где не нужна история оклада)
	СформироватьДвиженияПоДругимНачислениям();
	
	// 4. Обработка Сторно (вытеснение)
	СформироватьСторноЗаписи();
	
	// Если были критические ошибки при формировании движений — не продолжаем
	Если Отказ Тогда Возврат; КонецЕсли;
	
	// 5. Первая запись (чтобы платформа увидела периоды действия и построила график)
	Движения.ВКМ_ОсновныеНачисления.Записать();
	
	// 6. Расчетные процедуры
	РассчитатьОклад();
	РассчитатьОтпускные(); // Если у вас есть отпуск
	
	// 7. Финальная запись с результатами расчетов
	Движения.ВКМ_ОсновныеНачисления.Записать();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедуры

Процедура СформироватьДвиженияПоОкладам(Отказ)
	
	// А. Определяем границы периода данных в документе
	МинимальнаяДатаНачала = Неопределено;
	МаксимальнаяДатаОкончания = Неопределено;
	
	ЕстьОклад = Ложь;
	Для Каждого Строка Из Начисления Цикл
		Если Строка.ВидРасчета = ПланыВидовРасчета.ВКМ_ОсновныеНачисления.Оклад Тогда
			ЕстьОклад = Истина;
			Если МинимальнаяДатаНачала = Неопределено Или Строка.ДатаНачала < МинимальнаяДатаНачала Тогда
				МинимальнаяДатаНачала = Строка.ДатаНачала;
			КонецЕсли;
			Если МаксимальнаяДатаОкончания = Неопределено Или Строка.ДатаОкончания > МаксимальнаяДатаОкончания Тогда
				МаксимальнаяДатаОкончания = Строка.ДатаОкончания;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Если Не ЕстьОклад Тогда Возврат; КонецЕсли;

	// Б. Строим таблицу интервалов через вложенный запрос
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ТЧ.Сотрудник КАК Сотрудник,
	|	ТЧ.ВидРасчета КАК ВидРасчета,
	|	ТЧ.ДатаНачала КАК ДатаНачала,
	|	ТЧ.ДатаОкончания КАК ДатаОкончания,
	|	ТЧ.ГрафикРаботы КАК ГрафикРаботы
	|ПОМЕСТИТЬ ВТ_ТЧ
	|ИЗ
	|	Документ.ВКМ_НачисленияЗарплаты.Начисления КАК ТЧ
	|ГДЕ
	|	ТЧ.Ссылка = &Ссылка
	|	И ТЧ.ВидРасчета = &ВидРасчетаОклад
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	СрезПоследних.Сотрудник КАК Сотрудник,
	|	СрезПоследних.Период КАК Период,
	|	СрезПоследних.Оклад КАК Оклад
	|ПОМЕСТИТЬ ВТ_История
	|ИЗ
	|	РегистрСведений.ВКМ_УсловияОплатыСотрудников.СрезПоследних(&МинимальнаяДатаНачала, Сотрудник В
	|		(ВЫБРАТЬ
	|			Т.Сотрудник
	|		ИЗ
	|			ВТ_ТЧ КАК Т)) КАК СрезПоследних
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	История.Сотрудник,
	|	История.Период,
	|	История.Оклад
	|ИЗ
	|	РегистрСведений.ВКМ_УсловияОплатыСотрудников КАК История
	|ГДЕ
	|	История.Период > &МинимальнаяДатаНачала
	|	И История.Период <= &МаксимальнаяДатаОкончания
	|	И История.Сотрудник В
	|		(ВЫБРАТЬ
	|			Т.Сотрудник
	|		ИЗ
	|			ВТ_ТЧ КАК Т)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	Вложенный.Сотрудник КАК Сотрудник,
	|	Вложенный.ДатаНачала КАК ДатаНачала,
	|	ЕСТЬNULL(ДОБАВИТЬКДАТЕ(Вложенный.СледующаяДата, СЕКУНДА, -1), ДАТАВРЕМЯ(3999, 12, 31)) КАК ДатаОкончания,
	|	Вложенный.Оклад КАК Оклад
	|ПОМЕСТИТЬ ВТ_Интервалы
	|ИЗ
	|	(ВЫБРАТЬ
	|		История.Сотрудник КАК Сотрудник,
	|		История.Период КАК ДатаНачала,
	|		История.Оклад КАК Оклад,
	|		МИНИМУМ(ИсторияСлед.Период) КАК СледующаяДата
	|	ИЗ
	|		ВТ_История КАК История
	|			ЛЕВОЕ СОЕДИНЕНИЕ ВТ_История КАК ИсторияСлед
	|			ПО История.Сотрудник = ИсторияСлед.Сотрудник
	|			И История.Период < ИсторияСлед.Период
	|	СГРУППИРОВАТЬ ПО
	|		История.Сотрудник,
	|		История.Период,
	|		История.Оклад) КАК Вложенный
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_ТЧ.Сотрудник КАК Сотрудник,
	|	ВТ_ТЧ.ВидРасчета КАК ВидРасчета,
	|	ВТ_ТЧ.ГрафикРаботы КАК ГрафикРаботы,
	|	ВЫБОР
	|		КОГДА ВТ_ТЧ.ДатаНачала > ВТ_Интервалы.ДатаНачала
	|			ТОГДА ВТ_ТЧ.ДатаНачала
	|		ИНАЧЕ ВТ_Интервалы.ДатаНачала
	|	КОНЕЦ КАК ПериодДействияНачало,
	|	ВЫБОР
	|		КОГДА ВТ_ТЧ.ДатаОкончания < ВТ_Интервалы.ДатаОкончания
	|			ТОГДА ВТ_ТЧ.ДатаОкончания
	|		ИНАЧЕ ВТ_Интервалы.ДатаОкончания
	|	КОНЕЦ КАК ПериодДействияКонец,
	|	ВТ_Интервалы.Оклад КАК Показатель
	|ИЗ
	|	ВТ_ТЧ КАК ВТ_ТЧ
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Интервалы КАК ВТ_Интервалы
	|		ПО ВТ_ТЧ.Сотрудник = ВТ_Интервалы.Сотрудник
	|		И ВТ_ТЧ.ДатаНачала <= ВТ_Интервалы.ДатаОкончания
	|		И ВТ_ТЧ.ДатаОкончания >= ВТ_Интервалы.ДатаНачала";
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Запрос.УстановитьПараметр("ВидРасчетаОклад", ПланыВидовРасчета.ВКМ_ОсновныеНачисления.Оклад);
	Запрос.УстановитьПараметр("МинимальнаяДатаНачала", МинимальнаяДатаНачала);
	Запрос.УстановитьПараметр("МаксимальнаяДатаОкончания", МаксимальнаяДатаОкончания);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		Движение = Движения.ВКМ_ОсновныеНачисления.Добавить();
		ЗаполнитьЗначенияСвойств(Движение, Выборка);
		Движение.ПериодРегистрации = Дата;
		
		Если Движение.ВКМ_Результат = 0 Тогда
			Сообщение = Новый СообщениеПользователю;
			Сообщение.Текст = "Для сотрудника " + Строка(Выборка.Сотрудник) + " не установлен оклад!";
			Сообщение.Сообщить();
			Отказ = Истина;
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры

Процедура СформироватьДвиженияПоДругимНачислениям()
	
	Для Каждого Строка Из Начисления Цикл
		// Оклад уже обработан в предыдущей процедуре
		Если Строка.ВидРасчета = ПланыВидовРасчета.ВКМ_ОсновныеНачисления.Оклад Тогда
			Продолжить; 
		КонецЕсли;
		
		Движение = Движения.ВКМ_ОсновныеНачисления.Добавить();
		Движение.ПериодРегистрации    = Дата;
		Движение.ПериодДействияНачало = Строка.ДатаНачала;
		Движение.ПериодДействияКонец  = Строка.ДатаОкончания;
		Движение.Сотрудник            = Строка.Сотрудник;
		Движение.ВидРасчета           = Строка.ВидРасчета;
		Движение.ГрафикРаботы         = Строка.ГрафикРаботы;
	КонецЦикла;
	
КонецПроцедуры

Процедура СформироватьСторноЗаписи()
	
	// Получаем записи, которые вытесняются нашими новыми движениями
	СторноЗаписи = Движения.ВКМ_ОсновныеНачисления.ПолучитьДополнение();
	
	Для Каждого СтрокаСторно Из СторноЗаписи Цикл
		Движение = Движения.ВКМ_ОсновныеНачисления.Добавить();
		
		// Копируем все поля
		ЗаполнитьЗначенияСвойств(Движение, СтрокаСторно);
		
		Движение.ПериодРегистрации = Дата; // Сторнируем в текущем периоде
		Движение.ПериодДействияНачало = СтрокаСторно.ПериодДействияНачалоСторно;
		Движение.ПериодДействияКонец = СтрокаСторно.ПериодДействияКонецСторно;
		
		// Инвертируем ресурсы
		Движение.Сторно = Истина;
		Движение.Результат = -СтрокаСторно.Результат; // Если ресурс называется Результат
		// Движение.ВКМ_Результат = -СтрокаСторно.ВКМ_Результат; // Проверьте имя ресурса в регистре!
		
		// Обычно отработанные дни тоже сторнируют, если они есть в ресурсах
		// Движение.ОтработаноДней = -СтрокаСторно.ОтработаноДней; 
	КонецЦикла;
	
КонецПроцедуры

Процедура РассчитатьОклад()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ДанныеГрафика.НомерСтроки КАК НомерСтроки,
	|	ЕСТЬNULL(ДанныеГрафика.ЗначениеФактическийПериодДействия, 0) КАК Отработано,
	|	ЕСТЬNULL(ДанныеГрафика.ЗначениеПериодДействия, 0) КАК Норма
	|ИЗ
	|	РегистрРасчета.ВКМ_ОсновныеНачисления.ДанныеГрафика(
	|			Регистратор = &Ссылка
	|			И ВидРасчета = &ВидРасчетаОклад) КАК ДанныеГрафика";
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Запрос.УстановитьПараметр("ВидРасчетаОклад", ПланыВидовРасчета.ВКМ_ОсновныеНачисления.Оклад);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		Движение = Движения.ВКМ_ОсновныеНачисления[Выборка.НомерСтроки - 1];
		
		// Показатель (размер оклада) у нас уже есть в движении
		Оклад = ?(Движение.Показатель = 0, 0, Движение.Показатель); 
		
		Если Выборка.Норма = 0 Тогда
			// ВАЖНО: Проверьте точное имя ресурса в вашем регистре расчета. 
			// Обычно это "Результат" или "Сумма". В прошлой версии кода было ВКМ_Результат.
			// Я использую стандартное "Результат". Если у вас другое - поправьте.
			Движение.Результат = 0;
		Иначе
			Движение.Результат = Оклад * Выборка.Отработано / Выборка.Норма;
		КонецЕсли;
		
		// Если есть ресурс ОтработаноДней
		// Движение.ОтработаноДней = Выборка.Отработано;
		
		// Обработка Сторно при перерасчете
		Если Движение.Сторно Тогда
			Движение.Результат = -Движение.Результат;
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура РассчитатьОтпускные()
	
	// Проверяем, существует ли предопределенный элемент Отпуск
	// Если нет - закомментируйте эту процедуру
	Попытка
		ВидРасчетаОтпуск = ПланыВидовРасчета.ВКМ_ОсновныеНачисления.Отпуск;
	Исключение
		Возврат; // Нет такого вида расчета
	КонецПопытки;

	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ДанныеГрафика.НомерСтроки КАК НомерСтроки,
	|	ЕСТЬNULL(ДанныеГрафика.ЗначениеФактическийПериодДействия, 0) КАК ДнейОтпуска,
	|	ЕСТЬNULL(База.РезультатБаза, 0) КАК СуммаБазы,
	|	ЕСТЬNULL(База.ОтработаноДнейБаза, 0) КАК ДниБазы
	|ИЗ
	|	РегистрРасчета.ВКМ_ОсновныеНачисления.ДанныеГрафика(
	|			Регистратор = &Ссылка
	|			И ВидРасчета = &ВидРасчетаОтпуск) КАК ДанныеГрафика
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрРасчета.ВКМ_ОсновныеНачисления.БазаВКМ_ОсновныеНачисления(
	|				&Измерения,
	|				&Измерения,
	|				,
	|				Регистратор = &Ссылка
	|				И ВидРасчета = &ВидРасчетаОтпуск) КАК База
	|		ПО ДанныеГрафика.НомерСтроки = База.НомерСтроки";
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Запрос.УстановитьПараметр("ВидРасчетаОтпуск", ВидРасчетаОтпуск);
	
	Измерения = Новый Массив;
	Измерения.Добавить("Сотрудник");
	Запрос.УстановитьПараметр("Измерения", Измерения);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		Движение = Движения.ВКМ_ОсновныеНачисления[Выборка.НомерСтроки - 1];
		
		СреднийЗаработок = 0;
		// Внимание: проверьте имена полей базы (РезультатБаза, ОтработаноДнейБаза). 
		// Они зависят от того, какие ресурсы вы включили в базовые.
		Если Выборка.ДниБазы > 0 Тогда
			СреднийЗаработок = Выборка.СуммаБазы / Выборка.ДниБазы;
		КонецЕсли;
		
		Движение.Результат = СреднийЗаработок * Выборка.ДнейОтпуска;
		
		Если Движение.Сторно Тогда
			Движение.Результат = -Движение.Результат;
		КонецЕсли;
		
	КонецЦикла;

КонецПроцедуры

#КонецОбласти