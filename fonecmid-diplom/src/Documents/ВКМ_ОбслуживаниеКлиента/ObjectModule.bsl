#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	// ДЛЯ ТЕЛЕГРАМ-УВЕДОМЛЕНИЙ
	
	НужноОтправитьУведомление = Ложь;
	
	Если ЭтоНовый() Тогда
		// Если документ новый - всегда отправляем
		НужноОтправитьУведомление = Истина;
	Иначе
		// Если документ уже был, проверяем изменились ли ключевые поля.
		Если ВКМ_Специалист <> Ссылка.ВКМ_Специалист 
			ИЛИ ВКМ_ДатаПроведенияРабот <> Ссылка.ВКМ_ДатаПроведенияРабот
			ИЛИ ВКМ_ВремяНачалаРабот <> Ссылка.ВКМ_ВремяНачалаРабот Тогда
			
			НужноОтправитьУведомление = Истина;
		КонецЕсли;
	КонецЕсли;
	
	Если НужноОтправитьУведомление Тогда
		
		// Формируем текст сообщения для бота
		ТекстСообщения = "Обновление заявки!" + Символы.ПС +
						 "Клиент: " + Строка(ВКМ_Клиент) + Символы.ПС +
						 "Специалист: " + Строка(ВКМ_Специалист) + Символы.ПС +
						 "Дата: " + Формат(ВКМ_ДатаПроведенияРабот, "ДЛФ=D") + Символы.ПС +
						 "Время: " + Формат(ВКМ_ВремяНачалаРабот, "ДЛФ=T") + Символы.ПС +
						 "Проблема: " + ВКМ_ОписаниеПроблемы;
						 
		// Создаем запись в справочнике уведомлений
		Уведомление = Справочники.ВКМ_УведомленияТелеграмБоту.СоздатьЭлемент();
		Уведомление.ВКМ_ТекстСообщения = ТекстСообщения;
		Уведомление.Записать();
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, Режим)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	Договоры.ВидДоговора КАК ВидДоговора,
		|	Договоры.ВКМ_ДатаНачала КАК ДатаНачала,
		|	Договоры.ВКМ_ДатаОкончания КАК ДатаОкончания,
		|	Договоры.ВКМ_СтоимостьЧасаРаботы КАК СтоимостьЧаса,
		|	ЕСТЬNULL(УсловияОплаты.ВКМ_ПроцентОтРабот, 0) КАК Процент
		|ИЗ
		|	Справочник.ДоговорыКонтрагентов КАК Договоры
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ВКМ_УсловияОплатыСотрудников.СрезПоследних(&Период, ВКМ_Сотрудник = &Сотрудник) КАК УсловияОплаты
		|		ПО (ИСТИНА)
		|ГДЕ
		|	Договоры.Ссылка = &Договор";
	
	Запрос.УстановитьПараметр("Договор", ВКМ_Договор);
	Запрос.УстановитьПараметр("Период", Дата);
	Запрос.УстановитьПараметр("Сотрудник", ВКМ_Специалист);
	
	Результат = Запрос.Выполнить();
	
	// 1.1. Проверка наличия договора
	Если Результат.Пустой() Тогда
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = "Не заполнен или не найден договор!";
		Сообщение.Поле = "ВКМ_Договор";
		Сообщение.УстановитьДанные(ЭтотОбъект);
		Сообщение.Сообщить();
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	Выборка = Результат.Выбрать();
	Выборка.Следующий(); 
	
	// 1.2. Проверка вида договора
	Если Выборка.ВидДоговора <> Перечисления.ВидыДоговоровКонтрагентов.ВКМ_АбонентскоеОбслуживание Тогда
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = "Выбранный договор не является договором абонентского обслуживания!";
		Сообщение.Поле = "ВКМ_Договор";
		Сообщение.УстановитьДанные(ЭтотОбъект);
		Сообщение.Сообщить();
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	// 1.3. Проверка срока действия
	Если ЗначениеЗаполнено(Выборка.ДатаНачала) И Дата < Выборка.ДатаНачала Тогда
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = "Договор еще не вступил в силу (Дата начала: " + Формат(Выборка.ДатаНачала, "ДЛФ=D") + ")";
		Сообщение.Поле = "ВКМ_Договор";
		Сообщение.УстановитьДанные(ЭтотОбъект);
		Сообщение.Сообщить();
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Выборка.ДатаОкончания) И Дата > Выборка.ДатаОкончания Тогда
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = "Срок действия договора истек (Дата окончания: " + Формат(Выборка.ДатаОкончания, "ДЛФ=D") + ")";
		Сообщение.Поле = "ВКМ_Договор";
		Сообщение.УстановитьДанные(ЭтотОбъект);
		Сообщение.Сообщить();
		Отказ = Истина;
		Возврат;
	КонецЕсли;

	
	Движения.ВКМ_ВыполненныеКлиентуРаботы.Записывать = Истина;
	Движения.ВКМ_ВыполненныеСотрудникомРаботы.Записывать = Истина; 
	
	СтоимостьЧаса = Выборка.СтоимостьЧаса;
	Процент = Выборка.Процент;
	
	СуммаВсегоПоДокументу = 0;
	ЧасыВсегоПоДокументу = 0;
	
	Для Каждого ТекСтрока Из ВКМ_ВыполненныеРаботы Цикл
		
		// Движения по взаиморасчетам с клиентом
		Движение = Движения.ВКМ_ВыполненныеКлиентуРаботы.Добавить();
		Движение.Период = Дата;
		Движение.ВКМ_Клиент = ВКМ_Клиент;
		Движение.ВКМ_Договор = ВКМ_Договор;
		Движение.ВКМ_КоличествоЧасов = ТекСтрока.ВКМ_ЧасыКОплатеКлиенту;
		Движение.ВКМ_СуммаКОплате = ТекСтрока.ВКМ_ЧасыКОплатеКлиенту * СтоимостьЧаса;

		ЧасыВсегоПоДокументу = ЧасыВсегоПоДокументу + ТекСтрока.ВКМ_ЧасыКОплатеКлиенту;
		СуммаВсегоПоДокументу = СуммаВсегоПоДокументу + (ТекСтрока.ВКМ_ЧасыКОплатеКлиенту * СтоимостьЧаса);
		
	КонецЦикла;
	
	// Движения по выработке сотрудника (для расчета ЗП)
	Если ЧасыВсегоПоДокументу > 0 Тогда
		ДвижениеЗП = Движения.ВКМ_ВыполненныеСотрудникомРаботы.Добавить();
		ДвижениеЗП.Период = Дата;
		ДвижениеЗП.ВКМ_Сотрудник = ВКМ_Специалист;
		ДвижениеЗП.ВКМ_ЧасовКОплате = ЧасыВсегоПоДокументу;
		ДвижениеЗП.ВКМ_СуммаКОплате = СуммаВсегоПоДокументу * Процент / 100;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти