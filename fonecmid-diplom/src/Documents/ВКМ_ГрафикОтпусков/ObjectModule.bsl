Процедура ОбработкаПроведения(Отказ, Режим)
    Движения.ВКМ_ПланируемыеОтпуска.Записывать = Истина;
    Для Каждого ТекСтрока Из ОтпускаСотрудников Цикл
        
        Движение = Движения.ВКМ_ПланируемыеОтпуска.Добавить();
        Движение.Период = Дата; 
        Движение.ВКМ_Сотрудник = ТекСтрока.Сотрудник;
        Движение.ВКМ_КоличествоДней = ТекСтрока.ВсегоДней; 

    КонецЦикла;
КонецПроцедуры